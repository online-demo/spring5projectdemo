args 和 @args 测试demo

args()
该函数接收一个类名，表示目标类方法入参对象是指定类（包含子类）时，切点匹配。
比如args(com.test.Waiter) 表示运行时入参是Waiter类型的方法，它和execution(* *(com.test.Waiter))的区别:
在于后者是这对类方法的签名而言的，而前者是针对运行时的入参类型而言。

@args()
该函数接收一个注解类的类名，当方法的运行时入参对象标注了指定的注解时，匹配切点。